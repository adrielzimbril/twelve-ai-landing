<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Project Instructions
- **Package Manager**: Always use `pnpm` for installs, scripts, and lockfile updates. Never use `npm` or `yarn`.
- **Git**: Commit frequently with descriptive messages. Push to GitHub when significant milestones are reached.
- **Collaboration**: The user edits alongside the agent. Treat unexpected local modifications as user work: keep them, review them, and build on top of them instead of deleting or reverting them.
- **README Pattern**: When updating the README for this challenge, keep the established structure from the reference README. Only adapt the day number, project name, product description, design rationale, key sections, stack, preview asset, and project-specific details to the current landing page.
- **Design Files**: Never commit or push `DESIGN.md`, `design.md`, or any files inside `.design/` or `.deisgn/`. These files must remain local-only even during full-project commits or history rewrites.
